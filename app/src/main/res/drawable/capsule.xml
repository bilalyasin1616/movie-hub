<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:topLeftRadius="40dp"
        android:bottomRightRadius="40dip"
        android:bottomLeftRadius="40dip"
        android:topRightRadius="40dip"
        /> <!-- increasing the value, increases the rounding. And as  TTransmit said, to make it like a capsule make the radius half of your button height -->
    <!--<solid android:color="@color/colorAccent"/> &lt;!&ndash; the button color &ndash;&gt;-->
    <gradient
        android:angle="0"
        android:startColor="@color/colorAccent"

        android:endColor="@color/colorPrimary"

        android:type="linear" />
</shape>